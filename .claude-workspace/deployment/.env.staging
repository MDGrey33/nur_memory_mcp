# MCP Memory Server - Staging Environment
# ============================================
# Usage: docker compose --env-file .env.staging up -d
#
# Secrets (OPENAI_API_KEY) come from base .env file
# This file only contains environment-specific overrides
#
# Ports (per ADR-005, +100 from prod):
#   MCP: 3101, ChromaDB: 8101, PostgreSQL: 5532

# ============================================================================
# ENVIRONMENT IDENTIFICATION
# ============================================================================
ENVIRONMENT=staging
COMPOSE_PROJECT_NAME=mcp-memory-staging

# ============================================================================
# PORT CONFIGURATION (Staging = Prod + 100)
# ============================================================================
MCP_PORT=3101
CHROMA_EXTERNAL_PORT=8101
POSTGRES_EXTERNAL_PORT=5532

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
POSTGRES_DB=events_staging
POSTGRES_USER=events
POSTGRES_PASSWORD=staging_secret
EVENTS_DB_DSN=postgresql://events:staging_secret@postgres:5432/events_staging

# ============================================================================
# WORKER CONFIGURATION
# ============================================================================
WORKER_ID=worker-staging-1

# ============================================================================
# LOGGING (More verbose for staging)
# ============================================================================
LOG_LEVEL=DEBUG
