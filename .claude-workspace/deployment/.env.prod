# MCP Memory Server - Production Environment
# ============================================
# Usage: docker compose --env-file .env.prod up -d
#
# Secrets (OPENAI_API_KEY) come from base .env file
# This file only contains environment-specific overrides
#
# Ports (per ADR-005):
#   MCP: 3001, ChromaDB: 8001, PostgreSQL: 5432

# ============================================================================
# ENVIRONMENT IDENTIFICATION
# ============================================================================
ENVIRONMENT=production
COMPOSE_PROJECT_NAME=mcp-memory-prod

# ============================================================================
# PORT CONFIGURATION (Production)
# ============================================================================
MCP_PORT=3001
CHROMA_EXTERNAL_PORT=8001
POSTGRES_EXTERNAL_PORT=5432

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
POSTGRES_DB=events
POSTGRES_USER=events
POSTGRES_PASSWORD=events
EVENTS_DB_DSN=postgresql://events:events@postgres:5432/events

# ============================================================================
# WORKER CONFIGURATION
# ============================================================================
WORKER_ID=worker-prod-1

# ============================================================================
# LOGGING
# ============================================================================
LOG_LEVEL=INFO
