# MCP Memory Agent Configuration

# MCP Server Endpoint
# Hostname or URL of the chroma-mcp server
MCP_ENDPOINT=chroma-mcp

# Memory Confidence Threshold
# Minimum confidence score [0.0-1.0] required to store a memory
MEMORY_CONFIDENCE_MIN=0.7

# History Tail Size
# Number of recent conversation turns to retrieve for context
HISTORY_TAIL_N=16

# Memory Top-K
# Number of memories to retrieve in semantic search
MEMORY_TOP_K=8

# Memory Rate Limit
# Maximum number of memories to store per time window
MEMORY_MAX_PER_WINDOW=3

# Context Token Budget (optional)
# Maximum tokens for assembled context (leave empty for no limit)
# CONTEXT_TOKEN_BUDGET=4000

# Logging Level
# One of: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO
